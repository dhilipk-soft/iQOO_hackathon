package com.studylens.input.focus

import com.studylens.shared.NotificationEvent

class NotificationCollector {
    suspend fun getInterruptionEvents(): List<NotificationEvent> {
        // Notification listener event collector
        return emptyList()
    }
}
